import React from "react";
import './userInfo.css';

function UserInfo (props) {
    return (
        <div>
            <p>User information details with ability to update info</p>
        </div>
    );
}

export default UserInfo;